// Public surface of the MenuBar feature folder: the macOS-style top menu bar. It composes
// the shared flat leaf `OSDropdown` (which stays in components/ — used beyond this folder).
// Importers use `@/components/MenuBar`.
export { default as MenuBar } from './MenuBar.vue'
