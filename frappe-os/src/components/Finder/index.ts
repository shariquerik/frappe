// The Finder (ADR-0024): the OS's cross-app navigator and the principal drag-source for Placements.
export { default as Finder } from './Finder.vue'
// Its title-bar nav (title + search), slotted into the window chrome by OSWindow.
export { default as FinderToolbar } from './FinderToolbar.vue'
